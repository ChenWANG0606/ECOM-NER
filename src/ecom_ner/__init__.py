"""清理后电商 NER 项目的包入口。

这个文件刻意保持很轻，只暴露最基础的公共符号，避免轻量脚本在导入时
额外拉起训练相关依赖。

当前提供的符号：
- ``UNIFIED_LABELS``：项目默认使用的统一标签集合，数据准备、训练、
  评估和推理都会使用它。

设计说明：
- 这里不导入模型和训练相关模块，这样像数据准备脚本这类轻量流程在导入
  ``ecom_ner`` 时，不需要提前安装 ``transformers`` 或加载模型依赖。
"""

from .labels import UNIFIED_LABELS

__all__ = ["UNIFIED_LABELS"]
